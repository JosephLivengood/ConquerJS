import React from 'react';
import {render} from 'react-dom';

default export class Sample extends React.Component {
  render () {
    return <p>Sample: I'm used in a lot of scenes, so instead of having repeat components that you'd have to update on multiple scenes, we put me here!</p>;
  }
}